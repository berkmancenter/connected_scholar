<%= form_for([@document, @document.comments.build]) do |f| %>
  <div class="field">
    <%= f.label :comment_text %><br />
    <%= f.text_area :comment_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>